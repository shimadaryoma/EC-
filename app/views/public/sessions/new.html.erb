<%= render "public/shared/links" %>  
  <div class="container">
    <div class="column mt-5">
      <h2 class="col-3 h-15 bg-light px-5 text-center">ログイン</h2>
        <p class="bg-light col-3 text-center mt-3 mx-5">会員の方はこちらからログイン</p>
        <div class="row">
          <div class="mx-auto col-sm-10 col-md-10 col-lg-10 ">
              <%= form_with model: @customer, url: customer_session_path do |f| %>
  
              <div class="field row">
                <div class="px-3 pt-3">
                  <%= f.label :メールアドレス %>
                </div>
                <div class="px-3 pt-3">
                  <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
                </div>
              </div>
  
              <div class="field row">
                <div class="px-3 pt-3">
                  <%= f.label :パスワード %>
                </div>
                <div class="px-5 pt-3">
                  <%= f.password_field :password, autocomplete: "current-password" %>
                </div>
              </div>
  
                <div class="actions pt-3 w-60 col-5 text-center">
                  <%= f.submit "ログイン", class: 'btn btn-info btm-sm btm-block sign-in' %>
                </div>  
              <% end %>
              
              <p class="bg-light col-4 mt-5 text-center">会員登録がお済みではない方</p>
              <p class="px-5">
                <%= link_to "こちら", new_customer_registration_path %>
                からログインしてください。
              </p>
          </div>
        </div>
    </div>
  </div>